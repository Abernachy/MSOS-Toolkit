import { createContext } from 'react'

//Scratching the idea for now.  4/7/2022
// Thought about implementing context for the data utils, but it may not be really needed.  I'll save that idea.
// If the app gets slow in production, its an idea worth looking into; though we aren't pulling from a backend

export const stuff = useContext(null)
