import { useReducer } from 'react'
import * as resources from '../constants/resourceConstants'
import { initialState } from '../initialState/initialState'

const resourceReducer = (state, action) => {}

export default resourceReducer
